# Weather Forecast API

Микросервис на **NestJS** для получения прогноза погоды с точностью до часового пояса. Использует данные **Yr.no** и геокодинг **OpenStreetMap Nominatim**.

## 🚀 Быстрый старт (Docker)

Самый простой способ запустить приложение — использовать Docker Compose.

1. Создайте файл `.env` (можно скопировать пример):
   ```bash
   cp .env.example .env
   ```
2. Запустите контейнеры:
   ```bash
   docker-compose up -d
   ```
3. Приложение будет доступно по адресу `http://localhost:3000`.

## 🛠 Функциональность

*   **Умный выбор времени:** Алгоритм автоматически определяет часовой пояс координат и возвращает прогноз, максимально близкий к 14:00 *местного* времени.
*   **Кэширование:** Используется **Redis**. Поддерживаются заголовки `If-Modified-Since` и ответы `304 Not Modified` для экономии трафика.
*   **Геокодинг:** Поиск координат по названию города.
*   **Безопасность:** Rate Limiting для исходящих запросов к API провайдеров (защита от бана).
*   **Логирование:** Структурированные JSON-логи (Pino) с корреляционными ID.

## 📚 Документация API (Swagger)

Полная интерактивная документация доступна после запуска приложения:

👉 **[http://localhost:3000/api/swagger](http://localhost:3000/api/swagger)**

### Основные эндпоинты

Приложение использует **версионирование URI** (`v1.0`).

*   `GET /api/v1.0/weather?lat=44.81&lon=20.45` — Прогноз погоды по координатам.
*   `GET /api/v1.0/geo/search?name=Belgrade` — Поиск локации по названию.
*   `GET /api/health` — Health check для мониторинга.

## 🧪 Тестирование

Проект содержит Unit и Integration тесты, покрывающие сложную бизнес-логику (выбор таймзон, стратегии кэширования).

Запуск тестов:
```bash
npm run test
```

## 📦 Сборка для продакшена

Для деплоя предусмотрен отдельный файл конфигурации:

```bash
docker-compose -f docker-compose.prod.yml up -d
```
Приложение готово к работе в облаке (поддерживает `REDIS_URL`, `PORT` и graceful shutdown).
